import jsutil from 'typescript-coverage-repro/utils/jsutil';
import { module, test } from 'qunit';

module('Unit | Utility | jsutil', function() {
  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = jsutil();
    assert.ok(result);
  });
});
