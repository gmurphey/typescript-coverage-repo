import tsutil from 'typescript-coverage-repro/utils/tsutil';
import { module, test } from 'qunit';

module('Unit | Utility | tsutil', function() {
  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = tsutil();
    assert.ok(result);
  });
});
